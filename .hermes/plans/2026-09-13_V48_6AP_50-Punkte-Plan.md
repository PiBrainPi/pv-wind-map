# 50-Punkte-Plan V48 — 6 Arbeitspakete (Recherche / Planung / Umsetzung / Überprüfung) — FINAL

> **Anlass:** User-Revision 13.09. (Morgen). AP: 1 (Historie-Note löschen) · 2 (Chart-1-Legende
> unter Titel) · 3 (2 Ratio-Heatmaps MW/km² + MW/Einwohner) · 4 (Outlier 1990/2004 aus
> Y-Skalierung) · 5 („🗺️ Auf Karte"-Buttons auf Betreiber/Hersteller/Typ/Landkreis/NAP) ·
> 6 (Doku as-built). Kein Push/Deploy.

## Phase A — Recherche ✅ (durchgeführt vor Plan-Finalisierung)
1. AP1: note-Zeile Chart 1 (6543) — buildChart rendert bei note:'' bereits nichts (conditional) ✓
2. AP2: Canvas-Legende in drawStackedBar (fillRect W-80/PAD_T, 2×) überlappt Chartkopf ✓
3. AP3: blData[bl][y] je BL/Jahr (val = mw/anzahl je nach Modus); heatmap-Muster (heatColor,
   zubau-heatmap CSS) wiederverwendbar; BL-Namen identisch mit Destatis-Schreibweise ✓
4. AP4: drawRateChart maxR = Math.max(...) über ALLE Raten; drawCumChart analog — 1990/2004
   sprengen Skala; Label-Blöcke vorhanden (V21.6-Über-Punkt-Labels) ✓
5. AP5-Kernbefunde:
   - renderBetreiber: rows = gefilterte Betreiber (combo-aware); Gruppen-Rows separat;
     Klick-Zeile → selectBetreiber(name) (renderMarkers + fitBounds + closeStats).
   - renderHersteller: rows combo-aware; Klick → selectHersteller (u.herst exakt).
   - renderTyp: rows combo-aware (typ+herst Haystack); Klick → selectTyp(herst, typ).
   - renderLandkreise: rows combo-aware (r.lk); Klick → selectGeo({typ:'lk', name}).
   - renderNAP: rows combo-aware über nm/bl/m; Einzel-Klick → focusNAPOnMap (NAP-Gruppen-Pfad
     via __PVWIND_NAP__ → lid → _bffShowSet + openNapPanel; Fallback flyTo + Circle).
   - Gemeinsame Karten-Mechanik: renderMarkers(units) + fitBounds + closeStats() —
     combo-fähige Show-Funktion kann pro Tab die sichtbaren rows → units mappen.
   - TDZ-Warnung: _bffShowActive/_bffShowSet sind früh deklariert (2413–2419) — Nutzung ok.
6. AP6: Doc-Anker wie gehabt.

## Phase B — Planung (Punkte 7–12) ✅
7. AP1: note:'' (nur Chart 1) — erledigt in Phase C.
8. AP2: Canvas-Legende aus drawStackedBar entfernen; HTML-Legende (m-dot Wind/PV) nach zc-sub.
9. AP3: ZUBAU_BL_STATS (16 BL + AWZ, Destatis 31.12.2024); generische renderZubauRatioHeatmap
   (kumuliert Wind+PV je BL/Jahr ÷ km² bzw. ÷ Einwohner ×1000); 2 neue Blöcke unter Chart 4;
   nur im MW-Modus; AWZ-Zeile „—" (keine Landfläche/Einwohner).
10. AP4: outlierAwareBounds() (p90/p10-Quantil, Aktivierung nur wenn max > 1.5×limit);
    drawRateChart: maxR aus gefilterten Werten, Punkte/Linie an Grenze clippen, Labels > maxR
    weglassen; drawCumChart analog (maxR * 1.2 bleibt).
11. AP5: NEU showComboOnMap(kind) Helfer + je Tab ein 🗺️-Button (neben count-div):
    - betreiber: sichtbare Einzel-Rows (data-betreiber) ∪ Gruppen-Rows → units via u.ab;
      Combo: rows sind bereits gefiltert → alle rows-Units.
    - hersteller/typ: rows → units via u.herst (+u.typ), u.t === 'wind'.
    - landkreis: rows → units via u.lk ∈ rowLks.
    - nap: rows (gematchte NAPs) → alle Units, deren lid zu diesen NAPs gehört (via
      __PVWIND_NAP__ nap→lid-Index; Fallback: Unit-Name-Enthält-NAP-Name entfällt).
    - Button sichtbar nur bei Query ≥ 2 Zeichen; Klick → renderMarkers(units) + fitBounds +
      closeStats() + Suchfeld-Beschriftung; disabled bei 0 Units.
12. AP6: Doku (beide Projekte), Stale-Grep, Revision, human-share, Skill, Memory.

## Phase C — Umsetzung
13. ✅ AP1-Patch (note:'').
14. ✅ AP2-Patch (Canvas-Legende raus, HTML-Legende rein).
15. ✅ AP3-Patch A (ZUBAU_BL_STATS + renderZubauRatioHeatmap).
16. ✅ AP3-Patch B (2 Heatmap-Blöcke + Renderaufrufe, nur MW-Modus).
17. ✅ AP4-Patch (outlierAwareBounds + drawRateChart + drawCumChart geclippt; /tmp-Skripte wg.
    Whitespace-Fuzzy-Failures des patch-Tools).
18. AP5-Patch A: showComboOnMap() + Buttons in initStats-HTML (5 Tabs) + Handler.
19. AP5-Patch B: Verdrahtung je Renderfunktion (Rows sammeln + Button-Status).
20. node --check + Anker-Greps.
21. Build dist + singlefile.
22. Browser-Verifikation Multi (v48t1).
23. Browser-Verifikation Singlefile (v48s1).
24. AP6-Doku via /tmp-Skript.

## Phase D — Überprüfung (Akzeptanzkriterien)
25. AP1: Chart-1-Note weg, Charts 2+3 Notes da.
26. AP2: kein fillRect-Legende im Canvas; HTML-Legende unter Titel.
27. AP3: 3 Heatmaps; MW/km² plausibel (SH ~0.1–0.2, BY ~0.05); MW/1.000 Ew. plausibel (NI/NW
    vorne); AWZ „—"; eigene Farbskalen.
28. AP4: Rate-Chart maxR ohne 1990/2004 (z. B. ~20–30 % statt 300 %); Clip-Punkte ohne Label;
    Cum-Chart analog.
29. AP5: Buttons nur mit Query sichtbar; Klick → Marker + Zoom + Panel zu; 0 JS-Errors;
    Regression: Betreiber-Klick, NAP-Einzel-Klick, LK-Zeilen-Klick unverändert.
30. AP6: Doku as-built beide Projekte; Revision in iterations/ + human-share; KEIN Push/Deploy.
