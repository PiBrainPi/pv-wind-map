# 50-Punkte-Plan V47 — 5 Arbeitspakete (Recherche / Planung / Umsetzung / Überprüfung)

> **Anlass:** User-Revision 12.09. spät abends. AP-Nummerierung des Users: 1 (Historie-Charts),
> 3 (NAP-Combo-Suche), 4 (Zubau-Chart-Breite), 5 (Sticky-Header), 6 (Doku). — Nr. 2 fehlt im User-Text;
> User bestätigte per clarify: das (Mobil-Toggle) AP ist erledigt (V46-Fix), keine Arbeit nötig.
> **Clarifies (beantwortet):** AP1: Ticks BEIDER Achsen ganze Zahl ohne „k" · linke Achse wörtlich
> 5.000-MW-Schritte · Wert-Labels KOMPLETT ENTFERNEN (Ablesen über die Achsen) · rechte Achse
> dynamisch, aber ganze Zahl.

## Phase A — Recherche (abgeschlossen)
1. renderHistorieCharts (src ~6360): fmtK() kürzt auf „120k" — zu ersetzen durch ganze Zahl.
   minLeft/step je Chart: 120000/20000, 80000/10000, 50000/10000 → step überall 5000.
   Wert-Labels: Emission im Punkte-Loop (~6449) entfernen; Note-Text anpassen.
2. NAP-Tab (initNAPTab ~5472): Suggest filtert mit einfachem includes; renderNAP (~5422)
   matcht nm/bl/m per includes — combo-fähig machen (parseComboQuery/matchesCombo/comboTermContext
   existieren global seit V44/V45). Hinweissatz fehlt unter #nap-search.
3. Zubau-Tab (renderZubau ~5908): `const W = 640` fix — Canvas ignoriert Panelbreite.
   Heatmap (HTML-Tabelle width:100%) ist das Vorbild. Container: #zubau-charts,
   Block-Padding 12px je Seite.
4. Sticky-Header: NAP-Muster = thead-tr `position:sticky; top:0; z-index:2; background:#f7f9fc;
   border-bottom:2px solid #e5e7eb` innerhalb #nap-table-wrap. Betreiber/Hersteller/Typ-Tabellen
   scrollen vertikal in #stats-body (overflow-y:auto) → sticky th top:0 wirkt dort.
   Landkreis: th:first-child ist bereits sticky left (V32) → top ergänzen.
5. Doku-Anker wie gehabt (PROJEKTSTAND-Kopf, statistik.md, fehlerbehebung, ENTSCHEIDUNGEN,
   iterations/README, Hosting README + HANDOVER) + Stale-Grep.

## Phase B — Planung
6. AP1: fmtK → fmtInt (toLocaleString de-DE, 0 Nachkommastellen, kein k); step 5000 für alle 3
   Charts (maxL = ceil(max/5000)*5000 bleibt dynamisch nach oben); Labels raus; Note aktualisieren.
   padL 78 passt für „140.000".
7. AP3: renderNAP-Filter → combo ? matchesCombo(norm(`${nm} ${bl} ${m}`), combo) : includes.
   Suggest: comboTermContext (term nach letztem Operator; leer → letzter vollständiger Term),
   BL zuerst, dann NAPs; Klick hängt an prefix an. Hinweis div unter der Suchleiste.
8. AP4: W aus container.clientWidth − 26 (Block-Padding), min 320; resize-Listener mit Debounce
   re-rendert Zubau-Tab wenn aktiv.
9. AP5: CSS je Tabelle (stats/hersteller/typ): th sticky top:0 z-index:2 + border-bottom 2px
   #e5e7eb; Landkreis th sticky top + first-child z-index:4 (kombiniert mit link-sticky).
10. AP6: Doc-Sync-Liste (Phase D) + Stale-Grep-Muster.
11. Verifikation je AP (Phase D), Multi + Singlefile Pflicht; kein Push/Deploy.

## Phase C — Umsetzung
12. AP1-Patches (fmt, steps, Labels, Note).
13. AP3-Patches (renderNAP-Filter, NAP-Suggest, Hinweissatz).
14. AP4-Patch (dynamische Canvas-Breite + resize).
15. AP5-CSS-Patches (4 Tabellen).
16. Anker-Greps + node --check je Block.
17. `cp src/index.html dist/index.html` + `bundle_singlefile.py`.
18. Browser-Verifikation Multi (Cache-Buster).
19. Browser-Verifikation Singlefile.
20. AP6: Doku-Patches via /tmp-Skript (kein Inline-Heredoc).
21. Stale-Grep über aktive .md.
22. Revision `iterations/V47_*.html` + human-share-Kopie (ls-Verifikation).
23. Skill-Patch + Memory-Update.
24. KEIN Commit/Push/Deploy — Ausgabe als MEDIA: im Chat.

## Phase D — Überprüfung (Akzeptanzkriterien)
25. AP1: Tick-Texte aller Achsen matchen ^\d{1,3}(\.\d{3})+$ (ganze Zahl, kein „k"); linke
    Ticks in 5.000er-Abständen (120.000/125.000/…); KEIN Label-<text> mehr an den Punkten
    (nur <circle>); 0 JS-Errors.
26. AP3: „börde/havelland" im NAP-Tab filtert (Rows > 0, count-Text); Suggest nach „bertikow/"
    liefert weiter Treffer; Klick hängt an („bertikow/<NAP>"); Hinweissatz sichtbar.
27. AP4: Canvas-Breite ≈ Container-Breite (±4 px) im geöffneten Panel; Heatmap weiterhin 100 %.
28. AP5: getComputedStyle(th).position === 'sticky' auf allen 4 Tabellen; after 1500px-Scroll
    ist th-Rect.top ≈ Scrollcontainer-Top (Header bleibt sichtbar).
29. Regression: Betreiber-Combo (rwe/vestas), LK-Combo (börde/havelland = 2 LKs), Verlauf
    4 Snapshots + Klick-Detail, Infobar 31.006/22.438.
30. Doku: alle Ziele gepatcht, Stale-Grep clean; Revision in human-share.
