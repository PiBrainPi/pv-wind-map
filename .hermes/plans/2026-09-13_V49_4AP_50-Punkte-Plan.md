# 50-Punkte-Plan V49 — 4 Arbeitspakete (Recherche / Planung / Umsetzung / Überprüfung)

> **Anlass:** User-Revision 13.09. (Vormittag, nach V48). AP1 🗺️-Buttons ÜBER die Tabellen
> (5 Tabs) · AP2 Landkreis-Header 1. Spalte sticky-fix · AP3 Größenklassen: Default „Parks
> aggregiert" · AP4 Doku as-built. **Kein Push/Deploy ohne Freigabe.**

## Phase 1 — Recherche (P1–P10)
1. AP1: Button-Positionen — grep `show-map-*`: Betreiber 1832, Hersteller 1866, Typ 1910,
   Landkreis 1995 = UNTER count-div; NAP 2013 = bereits ÜBER nap-table-wrap ✓.
2. AP1: Tabellen-Anker je Tab: `#stats-table`, `#hersteller-table`, `#typ-table`,
   `#landkreis-table`, `#nap-table` (in `#nap-table-wrap`).
3. AP2: LK-CSS 1261–1277: `thead tr {position:sticky;top:0}` + `th:first-child {position:sticky;left:0}`.
4. AP2: Root-Cause-Hypothese: Chrome wendet bei th mit eigener sticky-Definition (left)
   NUR diese an — das top-Sticky des tr wirkt auf die Eckzelle nicht → „Landkreis"-Header
   scrollt weg, übrige Spalten bleiben.
5. AP3: State `groesseBasis: 'einheiten'` (Zeile 3998) + HTML `active` am Einzelanlagen-Button
   (1926).
6. AP3: Umschalter-Logik 4353–4360 (click → classList toggle + renderGroesse) — Default nur
   via HTML-Klasse + State-Init, kein JS-Eingriff nötig.
7. Prüfen: `groesse-basis-hint`/localStorage-Abhängigkeiten (keine Persistenz des Basis-Flags).
8. Build-Kette: cp → bundle_singlefile → 48 MB; node --check der Script-Blöcke.
9. Test-Reihenfolge wie V47/V48: Multi-Build zuerst, dann Singlefile-Regression.
10. Doku-Ziele: PROJEKTSTAND, statistik.md, fehlerbehebung.md (F-LK-ECCELL-STICKY),
    ENTSCHEIDUNGEN, iterations, Hosting README+HANDOVER.

## Phase 2 — Planung (P11–P20)
11. AP1-Strategie: /tmp-Skript verschiebt die 4 Buttons regex-basiert VOR das jeweilige
    `<table …>`; Style margin-top:6px → margin-bottom:6px; NAP unverändert (schon korrekt).
12. AP1: `_showMapBtn`/Klick-Logik unverändert (IDs stabil).
13. AP2-Strategie: Sticky auf th-Ebene verlagern: `thead th {position:sticky;top:0;z-index:2}`
    + `th:first-child {left:0;z-index:4}`; tr behält nur Hintergrund (kein position).
14. AP2: td:first-child left-sticky (V32) bleibt unberührt.
15. AP3: HTML `active` von Einzelanlagen→Parks-aggregiert; State `'einheiten'→'cluster'`.
16. Risiko-Check AP3: Title-Texte/Hint unangetastet; renderGroesse liest nur State.
17. Verifikations-Kriterien: (a) Button direkt über thead je Tab, (b) Klick-Regression
    rwe/vestas, (c) LK: th-Rect.top konstant beim Scrollen, (d) Größenklassen-Tab öffnet mit
    cluster aktiv, (e) 0 JS-Errors, (f) Singlefile identisch.
18. Reihenfolge: Umsetzung → node --check → Build → Multi-Tests → Singlefile → Doku → Revision.
19. Abbruchkriterien: falls AP2-Fix andere Header beeinträchtigt → tr-sticky Variante messen.
20. Kein Deployment; Revision nach iterations/ + human-share erst nach grünen Tests.

## Phase 3 — Umsetzung (P21–P40)
21. /tmp/v49_ap1_buttons.py: 4 Buttons verschieben.
22. P21 verifizieren (grep: Button-Zeile unmittelbar vor table-Tag).
23. patch: LK-CSS-Block 1261–1277 neu strukturieren (AP2).
24. grep-Verifikation AP2-CSS.
25. patch: AP3 HTML active-Klasse.
26. patch: AP3 State-Init groesseBasis.
27. node --check beider Script-Blöcke.
28. cp src → dist + bundle_singlefile.
29. http.server 8088 background.
30. Multi: ?v49t1=1 — Cookie+init, jsErrors-Collector.
31. AP1 Betreiber: Button-VOR-table prüfen + rwe/vestas Klick → Karte.
32. AP1 Hersteller/Typ/Landkreis: Button-Position + Counts (vestas/nordex, e-70/n117,
    börde/havelland).
33. AP1 NAP: Position verifizieren (schon oben) + bertikow/barnim Regression.
34. AP2: LK-Scroll-Test — getBoundingClientRect der Eckzelle vor/nach scrollTop 500/900.
35. AP2: andere 3 Tabellen-Header regressionsprüfen (sticky noch intakt).
36. AP3: Größenklassen-Tab frisch öffnen → Parks-aggregiert-Button .active + Summary cluster-
    konsistent; Umschalter-Regression (Klick auf Einzelanlagen funktioniert).
37. Historie-Regression (4 Snapshots, 3 Charts).
38. Singlefile ?v49s1=1: init, AP3-Default, AP1-Betreiber-Klick, 0 JS-Errors.
39. Server kill.
40. Revision iterations/V49_*.html + human-share-Kopie.

## Phase 4 — Doku + Überprüfung (P41–P50)
41. PROJEKTSTAND: V49-Kopf.
42. /tmp/v49_ap4_docs.py → statistik.md, fehlerbehebung.md, ENTSCHEIDUNGEN, iterations.
43. Hosting README-Changelog + HANDOVER-Status.
44. Skill-Patch V49-Sektion (inkl. F-LK-ECKZELL-STICKY-Pattern).
45. memory: V48→V49.
46. Finale Checks: grep-Anker (4 Buttons vor Tables, CSS th-sticky, State cluster).
47. Bestätigen: keine committeten/gepushten Änderungen (git status sauber, nur working tree).
48. Ergebnisse zusammenfassen (AP je Status + Zahlen).
49. Klickbare Datei ausgeben (MEDIA human-share).
50. Abschluss: Wiedereinstieg über PROJEKTSTAND V49-Kopf; offene Punkte notiert.
