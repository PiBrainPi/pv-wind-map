# 50-Punkte-Plan V51 — 3 Arbeitspakete (Recherche / Planung / Umsetzung / Überprüfung)

> **Anlass:** User-Wunsch 13.09. (nach V50). AP1: Zubau-Heatmap klickbar (Jahr → Karte,
> Zelle Jahr×BL → Karte) · AP2: 3 Balkendiagramme klickbar (Jahr → Karte) · AP3: Doku as-built.
> **Kein Push/Deploy ohne Freigabe.**

## Phase 1 — Recherche (P1–P10)
1. Heatmap-Struktur: `renderZubauHeatmap()` (6551) baut HTML-Tabelle (th=Jahre, td=Werte).
2. Balkendiagramme: `drawStackedBar` (6187) + `drawSingleBar` (6238) auf Canvas —
   Spalten-Geometrie: x = PAD_L + i*(plotW/n), Breite plotW/n (Hit-Test über Click-X machbar).
3. Karte-Mechanik vorhanden: `showComboOnMap()` (5643) — closeStats → renderMarkers →
   fitBounds → Suchfeld-Label. Wiederverwendbar als Muster.
4. Datenbasis: renderZubau aggregiert `allUnits` über `dateField` ('reg'|'inb', 5957) —
   Karte-Filter MUSS dasselbe Feld nutzen (Zahl-Konsistenz Chart ↔ Karte).
5. Technologie-Scopes: Chart 1 (Wind+PV) → u.t wind|pv; Chart 2 → pv; Chart 3 → wind;
   Heatmap → wind|pv (gemäß val()/valW()/valP()-Definitionen, 5994–5996).
6. Jahr-Quelle: `dateYear(u[dateField])` (5977) — gleiche Funktion im Filter verwenden.
7. BL-Quelle: `u.b || 'Unbekannt'` (5980) — td needs data-bl mit exakt diesem Key.
8. Re-Render-Zyklus: renderZubau setzt container.innerHTML neu → Bindings JE Render
   neu setzen (keine Leaks: alte Canvases werden verworfen).
9. Betroffene Sub-Elemente: zc-sub-Hinweise (6027/6039/6046/6053), CSS für Hover.
10. Doku-Betroffene: statistik.md, fehlerbehebung.md (ggf. Hinweis), ENTSCHEIDUNGEN,
    iterations, PROJEKTSTAND V52-Kopf, Hosting README+HANDOVER, README Zubau-Satz.

## Phase 2 — Planung (P11–P18)
11. Zentraler Helfer `showZubauYearOnMap(year, bl, tech)` nahe showComboOnMap:
    Filter (Jahr via dateYear + BL + Tech-Scope) → renderMarkers + fitBounds + Label.
12. `_zubauCtx.dateField` (module-level, von renderZubau gesetzt) — kein TDZ-Risiko
    (renderZubau läuft erst auf Tab-Klick, Deklaration Top-level).
13. Canvas-Klick-Binding in renderZubau nach dem Zeichnen: Spalten-Index aus
    Click-X (r.width-Skalierung) → years[i]; cursor:pointer; Charts 1/2/3 mit Tech-Scope.
14. Heatmap-Klicks: data-year auf th, data-year+data-bl auf td; Delegation auf
    zubau-heatmap-wrap (onclick überschreibend — kein Stacking bei Re-Render).
    Zusätzlich: Gesamt-Zeile (Jahressummen) ebenfalls klickbar = reiner Jahr-Klick.
15. Hover-Feedback: CSS outline auf th/td[data-year].
16. Hinweistexte: zc-sub aller 4 Charts + Heatmap ergänzen („Klick → Karte").
17. Verifikation: (a) Heatmap-Jahr-Header 2019 → nur-Anlagen-2019 auf Karte + Zahl
    plausibel vs. Chart; (b) Zelle Bayern×2023 → BL-Filter greift; (c) PV-Chart 2020 →
    nur PV; (d) Wind-Chart → nur Wind; (e) stacked → beide; (f) reg/inb-Sub-Tabs
    jeweils eigene Zahlen; (g) Regression Zubauraten/kumuliert unberührt; (h) 0 JS-Errors;
    (i) Singlefile identisch.
18. Build/Test-Plan: node --check → cp → bundle → 8090; t1 Multi, s1 Singlefile-Smoke.

## Phase 3 — Umsetzung (P19–P32)
19. patch: Helfer + _zubauCtx einfügen (nach showComboOnMap).
20. patch: renderZubau — _zubauCtx.dateField setzen.
21. patch: zc-sub Chart 1 Hinweis.
22. patch: zc-sub Chart 2 Hinweis.
23. patch: zc-sub Chart 3 Hinweis.
24. patch: zc-sub Heatmap Hinweis.
25. patch: Canvas-Klick-Bindings nach Chart-Zeichnung.
26. patch: renderZubauHeatmap — data-Attribute + Delegation + Gesamt-Zeile klickbar.
27. patch: CSS Hover/Cursor.
28. node --check beider Blöcke.
29. Build + Server 8090.
30. Browser t1: Szenarien 17a–17f (Zahlen via Suchfeld-Label verifizieren).
31. Singlefile s1: Smoke + ein Heatmap-Klick.
32. Server kill; Revision iterations/V51_*.html + human-share.

## Phase 4 — Doku + Überprüfung (P33–P50)
33. /tmp/v51_ap3_docs.py: statistik.md (V52-Sektion).
34. … fehlerbehebung.md (Hinweis-Kapitel falls Befund) oder Bestätigung aktuell.
35. … ENTSCHEIDUNGEN.md V51.
36. … iterations/README.md.
37. … PROJEKTSTAND.md V51-Kopf.
38. … README.md Zubau-Bullet (Klick-Interaktion).
39. … Hosting README Changelog.
40. … HANDOVER.md Statuszeile.
41. Skill-Patch pv-wind-map-development (V51-Sektion + Canvas-Klick-Muster).
42. memory-Update (V51-Stand).
43. Grep-Verifikation Code (data-year, showZubauYearOnMap).
44. Grep-Verifikation Doku (V51-Einträge).
45. git status sauber (working tree only, kein Push).
46. Zahlen-Plausibilisierung: Karte-Zahl vs. Chart-Wert je Szenario dokumentieren.
47. Re-Render-Check: Sub-Tab-Wechsel reg↔inb → Bindings neu + Zahlen korrekt.
48. MW/Anlagen-Toggle-Check: Klicks funktionieren in beiden Modi.
49. Statusbericht User (Umsetzung + Verifikationszahlen).
50. Klickbare V51-HTML im Chat (MEDIA human-share).
