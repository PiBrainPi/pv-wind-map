# 50-Punkte-Plan V45 — 4 Arbeitspakete (Recherche / Planung / Umsetzung / Überprüfung)

> **Anlass:** User-Revision 12.09. (Nachtrag zu V44): AP1 Chart-Feinschliff, AP2 Mobil-Toggle fixen
> (Button war beim Einklappen unsichtbar), AP3 Operator „+"→„/" + kontextbezogene Suggest,
> AP4 Doku as-built. Alle Clarifies beantwortet (siehe unten).
> **Vorab-Clarifies (User):** AP1 Skalen 120k/20k · 80k/10k · 50k/10k (Start = Beschriftung, dynamisch
> nach oben, darf über letzten Schritt hinaus); Ticks beider Achsen BLEIBEN 90° gedreht (V44), rechte
> Achse im selben Format wie linke; X-Datum 90° links gedreht UNTER der Achse; Punkte-Labels BEIDER
> Kurven 90° links gedreht ÜBER dem Punkt, Format Tausenderpunkt; Delta-Farbe = Farbe der
> Leistungslinie je Chart (blau/grün/orange), gestrichelt, Legende passt sich an.
> AP2: Toggle + Panel als fixe Bottom-Bar (Overlay), ausgeblendet bleibt NUR der Toggle sichtbar;
> PC-Ansicht OHNE Ausblend-Möglichkeit. AP3: strikt „/" = ODER (kein „+"), „&" = UND (& bindet
> stärker); Suggest liefert nach Operator WEITER Vorschläge für den Term nach dem letzten Operator,
> Auswahl hängt passend an; harte Prüfung inkl. Portfolio-/Gruppen-Suche.

## Phase A — Recherche (P1–P10)
1. ✅ renderHistorieCharts (6266–6390): buildChart-Signatur, niceMax, yTickText (Ticks rotiert), X-Labels gerade (6316), Punkte-Labels gerade (6331), Chart-Defs (6364–6388) mit minLeft 80000/55000
2. ✅ Legende erbt series.color (6342–6345) → Farbwechsel automatisch
3. ✅ Note-Text Chart 1 (6371) nennt „orange" → muss mit
4. ✅ AP2 Root-Cause: #toolbar-toggle absolute bottom:48px im #map-Container (1571–1580) — verschwindet beim Einklappen hinter Attribution/unterhalb Viewport; Panel #toolbar absolute bottom:88px (1117)
5. ✅ initToolbarToggle (3979–4002): isMobile via matchMedia 767, localStorage KEY, resize-Handler
6. ✅ parseComboQuery (6396–6403): split '+' → muss '/'; matchesCombo ok
7. ✅ Betreiber-Suggest _statsSuggest (4109–4166): Gruppen (_bffGrpCache) + Einzel mit nq=ganzer Query; Klick setzt value=kern/ab
8. ✅ LK-Suggest initLandkreisTab (4643–4672): hits via includes(q), Klick setzt ganzen Namen
9. ✅ Hersteller/Typ haben KEINE Suggest (nur clear-Button 4046/4064) → Suggest neu bauen (Daten: _stats.hersteller[].name, typ rows)
10. ✅ renderBetreiber Filterpfad (4327): combo → matchesCombo(norm(b.name)) — Portfolio-/Gruppen-Zeilen laufen über denselben Pfad (b.name = Kern/Brand-Name) → Combo wirksam; hart testen

## Phase B — Planung (P11–P18)
11. AP1 buildChart erweitern: Optionen {minLeft, step} → Tick-Erzeugung von minLeft in step-Schritten bis maxL = kleinste step-Vielfache ≥ max(maxWerte, minLeft); Gridlines analog; yL skaliert auf [minLeft..maxL] (NICHT 0-basiert!)
12. AP1 X-Labels: rotate(-90), anchor 'end', unterhalb Achse; padB 42→74; Punkte-Labels: rotate(-90 x y-12), anchor 'start' (läuft nach oben); padT 30→40
13. AP1 rechte Achse: Ticks fill #6b7280 (wie links), Titel fill #374151 (wie links) — nur Format, Werte weiter maxR-Skala
14. AP1 Delta-Farben: Chart1 #2563eb, Chart2 #059669, Chart3 #d97706; Note-Text anpassen (Farbe neutralt)
15. AP2 CSS ≤767px: #toolbar-toggle fixed bottom zentriert (immer sichtbar, z-index 1250); #toolbar fixed bottom über Button, max-height 62vh scrollable; body.toolbar-hidden #toolbar display:none; Desktop unverändert (display:none Button)
16. AP2 JS: Logik bleibt (matchMedia/localStorage); Zusatz: bei Panel öffnen Scroll-Verhalten unverändert; Button-Text/Pfeil weiter ▲/▼
17. AP3 Parser: split('/') ODER-Gruppen, '&' innerhalb; '+' = Literal (kein Operator); Erklärsätze 4× auf „/“ umstellen (Beispiele Börde/Havelland, wind&park)
18. AP3 Suggest generisch: helper `comboTermContext(raw)` → {prefix, term} (Term nach letztem / bzw. &); Betreiber-Suggest mit term statt nq; Klick hängt an: prefix + label; LK-Suggest gleiche Logik; NEU Hersteller/Typ-Suggest (einfache Namensliste, max 8, mit Kontext-Term) + Boxen im HTML

## Phase C — Umsetzung (P19–P32)
19. AP1: buildChart-Skala minLeft+step (Gridlines+Ticks)
20. AP1: yL/yR-Mapping auf [minLeft..maxL] umstellen (Basis minLeft, nicht 0)
21. AP1: X-Datum rotiert unter Achse
22. AP1: Punkte-Labels beider Kurven rotiert über Punkt
23. AP1: rechte Achse Format wie links
24. AP1: Delta-Farben je Chart + Legende (automatisch) + Note-Text
25. AP2: CSS-Umbau Bottom-Bar (Toggle fixed, Panel Overlay)
26. AP2: JS-Feinschliff (kein Drift zwischen CSS/JS, aria)
27. AP3: parseComboQuery auf '/'
28. AP3: Erklärsätze 4× (Betreiber/Hersteller/Typ/LK)
29. AP3: Betreiber-Suggest kontextbezogen (term + Anhängen)
30. AP3: LK-Suggest kontextbezogen
31. AP3: Hersteller-Suggest neu + Typ-Suggest neu (HTML-Boxen + JS)
32. AP4: Doku (Phase D)

## Phase D — Überprüfung (P33–P50)
33. Anker-Greps (Erwartungen V45) + node --check aller Blöcke
34. Browser Multi-File: 0 JS-Errors, Consent, Historie-Tab öffnen
35. AP1 Chart 1: Ticks 120k/140k(160k) vorhanden, 0-Tick WEG, yL-Basis minLeft (Leistungslinie nahe oben)
36. AP1 X-Daten rotiert + unterhalb (DOM: transform rotate, y > Achse)
37. AP1 Punkte-Labels rotiert über Punkt (beide Kurven, Tausenderpunkt)
38. AP1 Delta-Farben je Chart = Linienfarbe (SVG stroke + Legende)
39. AP1 rechte Achse Farbe/Format = links
40. AP2 Mobil-Emulation: matchMedia-Mock → Toggle sichtbar (fixed), Panel zu; Klick → Panel Overlay sichtbar; Klick → nur Button sichtbar; localStorage persistiert
41. AP2 Desktop: Button display:none, Panel immer da, keine Toolbar-Hidden-Klasse
42. AP3 „/": börde/havelland → 2 LKs; enercon/vestas → 3 Hersteller; rwe/vestas → RWE-Gruppe
43. AP3 „&": wind&park → UND; gemischt rwe/vestas&bedburg → (rwe ODER (vestas UND bedburg))语义 prüfen
44. AP3 Portfolio/Gruppen: rwe → Gruppe (brand/kern) im Suggest UND Tabelle; nach Operator: „rwe/" → Suggest weiter; Klick hängt an
45. AP3 Suggest Hersteller/Typ: „e-70/n117" → Vorschläge je Term; Klick anhängen
46. AP3 Regression: einfache Queries ohne Operator wie bisher; '+' jetzt Literal (Treffer nur wenn Name '+' enthält)
47. Klick-Detail Historie + Verlauf-Tabelle (Regression AP4 V44)
48. Bundle + Singlefile; Singlefile-Smoke (Consent + Historie)
49. AP4: Doku as-built (PROJEKTSTAND V45, statistik.md, fehlerbehebung, ROADMAP, ENTSCHEIDUNGEN, iterations, HANDOVER, Hosting-README, Skill-Patch)
50. Revision V45 → iterations/ + human-share; MEDIA im Chat; KEIN Push/Deploy

## Status
- P1–P18 ✅ (Recherche+Planung abgeschlossen)
- P19–P48 siehe laufende Umsetzung
